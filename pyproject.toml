[project]
name = "fastapi-htmx-demo"
version = "0.1.0"
description = "Add your description here"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn>=0.30.1",
    "jinja2>=3.1.4",
    "python-multipart>=0.0.9",
    "databases[aiosqlite]>=0.9.0",
]
readme = "README.md"
requires-python = ">= 3.8"

[tool.rye.scripts]
serve = "uvicorn app.main:app"
watch = { cmd = "uvicorn app.main:app --reload", env = { ORDER_SYSTEM_DEBUG = 1 } }
tailwind-build = "./tailwindcss --minify -i app/styles.css -o static/styles.min.css"
tailwind-watch = "./tailwindcss --watch -i app/styles.css -o static/styles.css"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src"]
